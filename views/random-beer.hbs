    <h1>random</h1>
<div>
    {{#each responseFromAPI}}
    <h2>{{this.name}}</h2>
    <p>{{this.description}}</p>
    <h3>{{this.tagline}}</h3>
    <p>{{this.food_pairing}}</p>
    <img src="{{this.image_url}}" alt="{{this.name}}">
    {{/each}}
</div>