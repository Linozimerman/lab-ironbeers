
    <h1>beers</h1>
{{!-- <div>
    {{#each beersFromApi}}
    <div>
    <h2>{{this.name}}</h2>
    <p>{{this.description}}</p>
    <h3>{{this.tagline}}</h3>
    <img src="{{this.image_url}}" alt="{{this.name}}">
    </div>
    {{/each}}
</div> --}}



{{#each beersFromApi}}
{{> partials}}
{{/each}}